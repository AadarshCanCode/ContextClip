# ContextClip v4 — Python prototype dependencies
# Install with: pip install -r requirements.txt

# Clipboard
pyperclip>=1.8.2

# Global keyboard listener
pynput>=1.7.6

# Screenshot / image processing
Pillow>=10.0.0

# System tray icon
pystray>=0.19.5

# Process / window inspection
psutil>=5.9.0

# Windows APIs (Win32) — Windows only
pywin32>=306; sys_platform == "win32"

# Optional: multiple monitor screenshot
mss>=9.0.1

# Web dashboard
flask>=3.0.0

# Optional: LLM compression (set OPENAI_API_KEY env var to enable)
openai>=1.0.0
