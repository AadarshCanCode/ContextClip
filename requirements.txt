# ContextClip v4 — Python dependencies
# Install: pip install -r requirements.txt

# Clipboard reading
pyperclip>=1.8.2

# Global keyboard listener (Ctrl+C / Ctrl+V detection)
pynput>=1.7.6

# Screenshot capture / image processing
Pillow>=10.0.0

# System process / window inspection
psutil>=5.9.0

# Windows APIs — Win32 window metadata, HWND, process info
pywin32>=306; sys_platform == "win32"

# Optional: multi-monitor screenshot support
mss>=9.0.1

# OpenRouter-powered context compression
# Set OPENROUTER_API_KEY env var to enable cloud compression.
# The OpenAI SDK is used only as OpenRouter's compatible client.
openai>=1.0.0

# Exa-powered clipboard reference search
# Set EXA_API_KEY env var to enable reference search.
exa-py==2.14.0
